<?php

declare(strict_types=1);

use Tests\TestCase;

uses(TestCase::class)->in('Feature', 'Unit');

// Global hooks or dataset definitions can be added here if needed

// Define test groups (optional)
